# image-rbac-proxy
This Go application acts as a reverse proxy for Quay.io. It enforces role-based access control (RBAC) to ensure that only authenticated and authorized users can pull repository resources.
